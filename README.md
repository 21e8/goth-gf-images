- `traits.js` => source for traits
- `calibrate.js` => faulty, should only calibrate if sum is larger than one. creates `fixed.json` which is then modified to export an object and rename `fixed.js` for easier import
-  
